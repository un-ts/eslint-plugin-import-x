lib
coverage
node_modules
tests/fixtures/malformed.js
tests/fixtures/with-syntax-error
tests/fixtures/just-json-files/invalid.json
tests/fixtures/typescript-d-ts/
# we want to ignore "tests/fixtures" here, but unfortunately doing so would
# interfere with unit test and fail it for some reason.
# tests/fixtures
